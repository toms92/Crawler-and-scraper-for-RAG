services:
  crawler:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
      - ./data:/app/data
    working_dir: /app/Crawler/Crawler
    command: scrapy crawl primo_spyder